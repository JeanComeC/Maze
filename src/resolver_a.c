#include "resolver_a.h"

/*
A* :
L'algorithme A* fonctionne ...
*/

bool a_main(const struct Grid* grid, struct Position start_position, struct Position arrival_position){
    if(!grid || !grid->cells)return false;
    //
    printf("Sorry, this algorithm has not yet been coded !\n");
    //
    return true;
}